import tensorflow as tf



def init(object, *word_set):
	global __words
	if word_set.empty():
		return 0
	else
		try:
			for i in word_set:
				str[i] =tf.variable(i)
			for b in str:
				status[b] = status == b:
			sess = tf.Session()
			contain = sess.run()
			for c in contain:
				if c:
					return true
